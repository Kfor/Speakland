# Implementation Agent

你是 Implementation Agent。在工作目录 `/Users/k/MyPlayground/aigc/Speakland/.weaver/worktrees/T1772514008997` 中完成以下任务。完成后将所有改动 git commit。

## 任务信息

- ID: T1772514008997
- 标题: Speakland Foundation — 项目脚手架 + 基础设施

### 需求全文

PRD: docs/prds/speakland-app.md
LLM Proxy PRD: docs/prds/llm-api-supabase-edge-function.md

从零搭建 Speakland Expo 项目的全部基础设施，不含用户可见功能界面。

具体范围：

1. Expo 项目初始化（expo-router file-based routing, TypeScript strict）
2. 安装所有依赖：@supabase/supabase-js, zustand, expo-av, expo-speech, react-native-reanimated, expo-auth-session (Google/Apple Sign-In), react-native-purchases (RevenueCat), 以及其他 PRD 要求的库
3. 项目配置：app.json（scheme, plugins, splash）, tsconfig.json, babel.config.js, .env.example（列出所有需要的环境变量）
4. Supabase 集成：
   - client 初始化（lib/supabase.ts）
   - 完整 DB schema SQL（stories, characters, chapters, dialogue_history, game_states, user_word_books, user_profiles 等表）
   - RLS policies（按用户隔离数据）
   - 本地开发用的 seed data
5. Supabase Edge Function: llm-proxy/index.ts
   - JWT 认证
   - 流式（SSE 透传）+ 非流式转发
   - 从服务端环境变量读取 LLM_API_KEY, LLM_API_ENDPOINT, LLM_MODEL
   - 参考 docs/prds/llm-api-supabase-edge-function.md 完整实现
6. Auth 完整流程：
   - Google Sign-In + Apple Sign-In
   - 登录/登出/session 管理/自动恢复
   - AuthContext provider
7. RevenueCat 初始化 + Purchases.configure
8. expo-router 导航结构：
   - app/_layout.tsx（root，providers wrap）
   - app/onboarding/ 目录（预留）
   - app/(tabs)/_layout.tsx（3 tab 结构：首页/角色/单词本）
   - app/scene/[id].tsx（预留）
   - app/paywall.tsx（预留）
9. 共享类型定义 types/（Story, Character, Chapter, DialogueMessage, GameState, WordEntry, UserProfile, OnboardingData 等）
10. 主题常量 constants/（颜色、字体、间距）
11. Zustand stores 骨架（useAuthStore, useOnboardingStore, useGameStore, useVocabularyStore）
12. lib/llmClient.ts 公共调用层：
    - callLlmProxy()（非流式）
    - streamLlmProxy()（流式 SSE）
    - 从 supabase session 获取 JWT
    - 未登录时 mock fallback
13. 确保 npx expo start 可正常启动（至少 web 端无报错）

不做：任何用户可见的功能界面和交互（Onboarding 页面、Tab 页面内容、场景 UI 等由后续任务实现）。导航结构只建骨架（空页面占位）。

### 验收标准

Expo 项目可启动，Supabase schema 已定义，auth + RevenueCat + LLM proxy 基础设施就绪，导航结构完整

### 项目知识（patterns）

（无）

### 项目记忆（memory）

# Project Memory (Mistakes Only)
## Mistakes
- (none)

### 资源状态

（无资源清单）

## 工作要求

1. **先判断任务可行性** — 任务描述与代码现状是否一致？当前 worktree 只保留了 `docs/` 目录，所有源代码已被 `chore: clean slate` 清除。你需要从零搭建。如有矛盾或不可行之处，在输出中明确说明。
2. **查阅 PRD** — 必须先阅读以下两个 PRD 文件，了解完整产品需求和技术规范：
   - `docs/prds/speakland-app.md` — 主产品 PRD
   - `docs/prds/llm-api-supabase-edge-function.md` — LLM Proxy Edge Function PRD
3. **阅读已有 docs** — 查看 `docs/` 下的其他文档（plans、story-references 等），了解项目历史和设计思路。
4. **实现需求** — 按照需求范围 1-13 逐项实现，确保每一项都覆盖到。
5. **编写或更新测试** — 核心判断：如果实现是错误的，测试能否发现？至少为关键模块（types, stores, llmClient, supabase client）编写基础测试。
6. **端口冲突检测** — 启动任何 dev server 前，必须先检查目标端口是否被占用（`lsof -i :<port>`）。发现占用时用环境变量指定空闲端口。**不要 kill 其他进程**。
7. **运行关键测试确认实现正确**
8. **测试失败时先当代码/配置 bug 修** — 只有确认服务本身不可达才上报为阻塞。
9. **自审** — 重新审视你的全部改动——生产环境会不会出问题？有没有多余的代码？根据自审结果修改。
10. **git add && git commit** 所有改动。使用清晰的 commit message。

## 关键技术决策指导

- **Expo SDK**: 使用最新稳定版（SDK 52+），基于 expo-router 的 file-based routing
- **TypeScript**: strict mode
- **状态管理**: Zustand（不是 Redux）
- **数据库**: Supabase（PostgreSQL）
- **Auth**: Supabase Auth with Google + Apple providers via expo-auth-session
- **支付**: RevenueCat (react-native-purchases)
- **LLM 调用**: 通过 Supabase Edge Function 代理，不直接从客户端调 LLM API
- **Supabase Edge Function**: Deno runtime, 使用标准 Web API (fetch, ReadableStream)
- **样式**: 使用 React Native StyleSheet，不引入额外 CSS-in-JS 库

## 输出要求

完成后，输出工作摘要，至少包含：
1. 做了什么改动（文件清单概要）
2. 关键决策理由
3. 测试结果
4. 遗留风险或已知限制
