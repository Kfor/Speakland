# Implementation Agent

你是 Implementation Agent。在工作目录 `/Users/k/MyPlayground/aigc/Speakland/.weaver/worktrees/T1772515590190` 中完成以下任务。完成后将所有改动 git commit。

## 任务信息

- **ID**: T1772515590190
- **标题**: Speakland 项目基础架构搭建

### 需求全文

Speakland 项目基础架构搭建。PRD: docs/prds/speakland-app.md。范围：
1. Expo (React Native + TypeScript) 项目初始化，安装全部依赖（supabase-js, revenucat, expo-auth-session, expo-speech 等）
2. Supabase 客户端配置 + 数据库 schema（users, vocabularies, stories, characters, progress 等表，含 RLS 策略）
3. Google/Apple Sign-In 登录集成
4. RevenueCat 付费墙集成（订阅页面 + 购买流程）
5. 3-Tab 底部导航骨架（故事/角色/设置）
6. 全局主题/样式系统 + 基础 TypeScript 类型定义
7. AI 对话服务层（OpenAI-compatible API，封装角色扮演 + 剧情上下文管理）

所有外部服务凭证通过 env 变量配置，提供 .env.example。

### 验收标准

Expo 项目可运行，Supabase 连接正常，Auth 和 RevenueCat 集成就绪，3-Tab 导航可切换

### 项目知识（patterns）

（无）

### 项目记忆（memory）

无错误记录。

### 资源状态

（无资源清单）

## 工作要求

1. **先阅读 PRD**：读取 `docs/prds/speakland-app.md` 了解完整产品需求，这是你的核心参考文档。同时查阅 `docs/plans/`、`docs/story-creation-guide.md`、`docs/story-references/` 了解更多上下文。

2. **查阅项目 Skills**：读取 `.claude/skills/` 目录下的文件（playbook, provisioning），了解技术栈规范与实现模式。

3. **判断任务可行性**：任务描述与代码现状是否一致？这是一个全新项目，worktree 中只有 `docs/` 目录，需要从零初始化。

4. **Expo 项目初始化**：
   - 使用 `npx create-expo-app@latest` 初始化项目（TypeScript template）
   - 注意：在 worktree 目录中初始化，项目文件应在 worktree 根目录下（不是嵌套子目录）
   - 安装所有必要依赖：
     - `@supabase/supabase-js` - Supabase 客户端
     - `@react-navigation/native`, `@react-navigation/bottom-tabs` - 导航
     - `expo-auth-session`, `expo-web-browser`, `expo-crypto` - OAuth
     - `expo-speech` - TTS
     - `react-native-purchases` - RevenueCat
     - `expo-secure-store` - 安全存储
     - 其他 PRD 中提到的依赖
   - 端口冲突检测：启动任何 dev server 前，先检查端口是否被占用

5. **Supabase 配置**：
   - 创建 Supabase 客户端配置（从环境变量读取 URL 和 anon key）
   - 创建数据库 schema SQL 文件（users, vocabularies, stories, characters, progress 等表）
   - 包含 RLS 策略定义
   - 使用 AsyncStorage 作为 Supabase auth 的存储

6. **认证集成**：
   - Google Sign-In 和 Apple Sign-In 的配置和流程
   - Auth context/provider 封装
   - 登录/注册页面骨架

7. **RevenueCat 集成**：
   - RevenueCat SDK 初始化
   - 订阅页面（Paywall）组件
   - 购买流程封装
   - 订阅状态管理

8. **3-Tab 导航**：
   - Stories（故事列表）
   - Characters（角色列表）
   - Settings（设置）
   - 底部 Tab 导航骨架实现

9. **主题/样式系统**：
   - 全局颜色、字体、间距定义
   - 主题 Provider
   - 基础 TypeScript 类型定义（Story, Character, User, Progress 等）

10. **AI 对话服务层**：
    - OpenAI-compatible API 封装
    - 角色扮演上下文管理
    - 剧情进度追踪
    - 消息历史管理

11. **环境变量**：
    - 创建 `.env.example` 包含所有必要的环境变量模板
    - 配置 `.gitignore` 排除 `.env`

12. **自审**：完成后重新审视全部改动——生产环境会不会出问题？有没有多余代码？

13. **提交**：`git add && git commit` 所有改动，commit message 简洁描述改动内容。

## 输出要求

完成后必须输出工作摘要，至少包含：
- 做了什么改动（文件列表概要）
- 关键决策理由
- 测试结果（如有）
- 遗留风险或需要注意的事项
